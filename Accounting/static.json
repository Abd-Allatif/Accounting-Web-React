{
  "rewrites": [
    {
      "source": "**",
      "destination": "/index.html"
    }
  ],
  "build": {
    "command": "npm run build",
    "directory": "build"
  },
  "deploy": {
    "startCommand": "npx serve -s build"
  }
}